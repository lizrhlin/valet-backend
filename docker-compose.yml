version: '3.8'

services:
  # PostgreSQL local (opcional - comentado pois usa Neon)
  # Descomente se quiser rodar Postgres localmente
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: valet-postgres
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: valet
  #   ports:
  #     - '5432:5432'
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ['CMD-SHELL', 'pg_isready -U postgres']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: valet-backend
    restart: unless-stopped
    ports:
      - '3000:3000'
    env_file:
      - .env
    # Se usar Neon, não precisa depends_on
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    command: sh -c "npx prisma migrate deploy && node dist/server.js"

volumes:
  postgres_data:
